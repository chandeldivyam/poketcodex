# Scripts

Utility scripts for local development and CI workflows.

- `poketcodex.sh`: local CLI wrapper (`init`, `up`, `down`, `status`, `logs`, `doctor`).
- `install.sh`: `curl | sh` bootstrap installer (release asset aware).
- `build-installer-assets.sh`: builds release assets (`install.sh`, source tarball, checksums).
